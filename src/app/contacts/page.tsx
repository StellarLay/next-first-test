import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contacts - Next first",
  description: "Contacts page",
};

const Contacts = () => {
  return <h1>Контакты</h1>;
};

export default Contacts;
